# List of courses
courses = ['Math', 'Geography', 'Chemistry']

# Using enumerate to get both the index and course name
for index, course in enumerate(courses):
    print(f"Course {index + 1}: {course}")
